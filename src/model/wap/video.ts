import { Element } from "./element"

export class WapVideo extends Element<'video'> {
    protected _src: string = ''
    // constructor(protected _sectionId: string) {
    //     super('video', _sectionId)
    // }
}